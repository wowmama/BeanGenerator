package ${beanPackageName};

import java.math.BigDecimal;
import java.util.Date;
import ${beanPackageName}.annotation.PrimaryKey;
import ${beanPackageName}.annotation.Column;
import ${beanPackageName}.annotation.Table;


/**
 * ${beanClassName}.java auto generated by Troy. ${generatedDate}
 */
@Table(name = "${tableName}")
public class ${beanClassName} {
${beanAllAttribute}

	public ${beanClassName}(){}
${keyConstructor}
${beanAllGetterAndSetter}

	@Override
	public String toString() {
		return String.format(
				"${beanClassName} [${beanFieldToString}]",${beanFieldToStringArgs});
	}
}
